# Flow CS Hub 배포 안내

**대상**: 송재풍 팀장 (사용자 본인 작업)
**목적**: GitHub Pages에 Flow CS Hub 배포
**최종 URL**: `https://coayay.github.io/flow-cs-hub/`

---

## 접근 정책 (2026-09-17 갱신)

- **비밀번호 없이 URL로 바로 접근**
- **검색엔진 노출 차단** (`robots noindex, nofollow`)
- URL을 CS 직원끼리만 공유 원칙

### ⚠ 유출 시 리스크
링크가 외부로 새면 담당자 실명·내선·직통번호·건물 위치가 그대로 노출됩니다. CS 직원 채널로만 공유하고, 학부모·외부 채널에는 절대 붙이지 마세요.

---

## 준비

### 필수
- GitHub 계정 (`coayay`)
- Git 설치 확인
  ```powershell
  git --version
  ```
  버전 나오면 OK. 안 나오면 https://git-scm.com 에서 설치.

### 배포 폴더 위치
```
C:\Users\flowedu1\신사업전략팀\03_AI_TF\03_CS상담플랫폼\챗봇 만들기\cs-db-v2\webapp-mockup\
```

배포 파일 목록:
- `index.html` (메인)
- `data/*.json` (33 + 66 + 49 = 148건)
- `README.md`
- `_config.yml`
- `.gitignore`

배포 제외 (`.gitignore`):
- `build_data.py` (내부용 스크립트)
- `설계문서.md` (내부 문서)
- `mockup.html` (테스트용 원본. index.html이 실제 진입점)

---

## 배포 단계 (10분)

### 1단계 — GitHub 리포지토리 생성

브라우저에서 `https://github.com/new` 접속.

| 항목 | 값 |
|---|---|
| Repository name | `flow-cs-hub` |
| Description | `수학의아침 CS 직원 통합 상담 지원 플랫폼` |
| Public / Private | **Public** |
| Initialize with README | 체크 **해제** |

→ 초록색 "Create repository" 클릭.

### 2단계 — 로컬에서 Git 초기화

PowerShell 열고 아래 붙여넣기:

```powershell
cd "C:\Users\flowedu1\신사업전략팀\03_AI_TF\03_CS상담플랫폼\챗봇 만들기\cs-db-v2\webapp-mockup"
git init
git branch -M main
git add .
git commit -m "초기 배포: Flow CS Hub v0.1"
git remote add origin https://github.com/coayay/flow-cs-hub.git
git push -u origin main
```

- 처음 `git push` 시 GitHub 로그인 창이 뜸
- 계정: `coayay`
- 비밀번호 대신 **Personal Access Token** 필요 (아래 참고)

#### Personal Access Token 발급 (한 번만)

1. 브라우저 → `https://github.com/settings/tokens`
2. "Generate new token" → "Generate new token (classic)"
3. Note: `flow-cs-hub-deploy`
4. Expiration: 90 days 또는 No expiration
5. Scopes: **repo** 체크 (전체)
6. 하단 "Generate token" 클릭
7. **표시된 토큰을 복사** (한 번만 보임)
8. PowerShell의 비밀번호 자리에 토큰 붙여넣기

토큰은 Windows 자격 증명에 저장되어 다음부터는 자동.

### 3단계 — GitHub Pages 활성화

리포지토리 페이지에서:

1. 상단 탭 **Settings** 클릭
2. 좌측 메뉴 **Pages** 클릭
3. Source 섹션:
   - Branch: **`main`**
   - Folder: **`/ (root)`**
   - **Save** 클릭
4. 상단에 URL 표시:
   ```
   Your site is live at https://coayay.github.io/flow-cs-hub/
   ```
   활성화까지 **1~3분** 대기.

### 4단계 — 접속 테스트

브라우저에서 `https://coayay.github.io/flow-cs-hub/` 접속.

체크 항목:
- [ ] 로그인 없이 바로 홈 대시보드 표시
- [ ] 3개 카드 (캠퍼스 · 수강료 · 매뉴얼) 표시
- [ ] 사이드바 항목 클릭 시 즉시 이동
- [ ] 검색창에 "광교 초등" 입력 → 결과 표시
- [ ] "ㄱㄱ ㅊㄷ" 초성 검색 작동
- [ ] 모바일에서 접속 시 햄버거 메뉴 표시

---

## 배포 후 작업

### 데이터 갱신 시

`data/*.json` 편집 → 커밋 → 푸시:

```powershell
cd "C:\Users\flowedu1\신사업전략팀\03_AI_TF\03_CS상담플랫폼\챗봇 만들기\cs-db-v2\webapp-mockup"
git commit -am "데이터 갱신: (변경 요약)"
git push
```

1~2분 내 사이트 반영.

원천 파일(엑셀·PDF)이 바뀌면 `build_data.py`로 JSON 재생성 후 커밋.

### 나중에 접근 통제가 필요해질 때

- **간단**: Netlify Basic Auth · Cloudflare Access 로 이관 (URL 유지)
- **완전 차단**: GitHub Pages Enterprise + IP 화이트리스트
- **간이**: 비밀번호 인증 재추가 (이전 코드 백업은 mockup.html에 있음)

---

## 문제 발생 시

### 배포 후 404 오류
- Settings → Pages에서 Source Branch가 `main`인지 확인
- 3~5분 대기 (첫 배포는 시간이 걸림)

### 데이터 안 나옴
- 브라우저 콘솔(F12) 확인
- `data/contacts.json` 등 404가 뜨면 파일 경로 문제
- `git status`로 data/ 폴더가 커밋됐는지 확인

### Push 시 403 오류
- Personal Access Token이 만료됐거나 권한 부족
- 새 토큰 발급 → Windows 자격 증명 관리자에서 기존 토큰 삭제 → 다시 push

---

## 배포 완료 체크리스트

- [ ] 리포지토리 `coayay/flow-cs-hub` 생성됨
- [ ] `git push` 성공
- [ ] GitHub Pages 활성화 (Settings → Pages)
- [ ] `https://coayay.github.io/flow-cs-hub/` 접속 확인
- [ ] 로그인 없이 데이터 · 검색 정상 작동
- [ ] CS 직원에게 URL 공지 (내부 채널 한정)
